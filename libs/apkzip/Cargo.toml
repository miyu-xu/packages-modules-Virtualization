[package]
name = "apkzip"
version = "0.1.0"
edition = "2021"

[lib]
path = "src/ziputil.rs"

[dependencies]
anyhow = "1.0"
bytes = "1.5"
zip = { version = "0.6", default-features = false, features = ["deflate"] }
